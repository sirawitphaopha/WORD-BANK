# SESSION 2026-07-20f — ผ่าตัดแบ่งไฟล์ WordBankApp.jsx (3055→300 บรรทัด) 2 รอบ + เก็บกวาด import

> เขียนให้ AI ที่ไม่เคยคุยกับพี่กันมาก่อนอ่านแล้วทำงานต่อได้ทันที (สมมติว่าไม่มีบริบทอะไรเลย)
> รอบนี้ทำบน **cloud agent** · รอบที่ 6 ของวัน 20 ก.ค. 2569 (ต่อจาก .md/-20b/-c/-d/-e)
> **3 commit ขึ้น main แล้ว** (พี่กันพิมพ์ "พุชเลย" ให้ 2 รอบแรก · "เก็บกวาด และพุช" ให้รอบเก็บกวาด)

## คนทำงาน (ทวนไว้ เผื่อ AI ตัวใหม่)
- **พี่กัน** = เจ้าของโปรเจกต์ · เภสัชกร ไม่มีพื้นฐานโค้ด แต่เข้าใจ logic ดี · อธิบายด้วยภาษาคน อย่าพ่นศัพท์เทคนิค · **ห้ามพูดรหัสหมวด c0–c8** เรียก "หมวด + เลข/ชื่อไทย" เท่านั้น
- **แคลร์** = ผู้ช่วย AI (ผู้หญิง ใช้ ค่ะ/นะคะ · แทนตัวว่า "แคลร์" เรียกผู้ใช้ว่า "พี่กัน")
- 🔑 **สไตล์รอบนี้:** งานนี้ไม่ใช่ปรับ UI แต่เป็น "จัดบ้านโค้ด" (refactor) — พี่กันอยากให้แบ่งไฟล์ยักษ์เป็นไฟล์เล็กๆ **จะได้แก้ง่ายตอน AI ทำงานต่อ** · เงื่อนไขเหล็ก = **หน้าตา/พฤติกรรมเว็บต้องเหมือนเดิมเป๊ะทุก pixel (byte-identical)** ไม่ใช่แค่ "ใกล้เคียง"

## 🎯 โจทย์
เดิมทั้งแอปอยู่ในไฟล์เดียว `components/WordBankApp.jsx` = **3055 บรรทัด** (คลาส React ตัวเดียว state + handler ~65 ตัว + render ทุกหน้า + ตัวช่วยแสดงผล) → ใหญ่จนแก้ยาก พี่กันสั่ง **"แบ่ง code เป็นส่วนย่อยๆ ขอละเอียด แล้วเอาโครงต้นไม้ (โครงไฟล์) ไปเขียนใน CLAUDE.md + README ด้วย"** → ต่อมาสั่งเพิ่ม **"เปลี่ยนเป็นเล็กที่สุด"** (แบ่งให้ไฟล์หลักเล็กสุดเท่าที่ทำได้)

ทำเป็น **2 รอบ (2 commit)** + เก็บกวาดอีก 1 commit:

## commit 1 (`9b356ba`) — รอบ "แยก view ต่อหน้า" (3055 → 1017 บรรทัด)
ยก **เมธอด render ของแต่ละหน้า** ออกจากคลาสไปเป็นฟังก์ชันแยกไฟล์ใน `components/pages/`
- แต่ละ view: `renderX(args){...}` (เมธอดในคลาส) → `export function renderX(app, args){...}` (ฟังก์ชันในไฟล์ pages/) · แปลง `this.`→`app.` · static เดิม `WordBankApp.thNum` → `thNum` (import จาก helpers)
- ในคลาส render() เรียก `renderX(this, ...)` แทน `this.renderX(...)`
- **ไฟล์ที่สร้าง:** `pages/{add,review,library,about,aiLog,aiTest,promptLog,modals}.jsx` + `pages/reportShared.jsx` (RP สี/ฟอนต์ + TEST_WORDS ชุดคำทดสอบ ที่ 2 หน้ารายงานใช้ร่วม) + `helpers.js` (VERSION · thNum · aiModel · shortDate · pathsOf — แยกไว้กัน circular import)
- **สคริปต์ที่ใช้ผ่าตัด:** `scratchpad/extract.js` (ตัดช่วงบรรทัดของเมธอด render แต่ละตัว, dedent, this→app, wrap เป็น export function)

## commit 2 (`e01f901`) — รอบ "แยก handler + โครง render" (1017 → 300 บรรทัด) = เล็กที่สุด
ยก handler ทุกตัว + โครง render() + ตัวช่วยแสดงผล ออกจากคลาส เหลือคลาสแค่แกน
- **`shell.jsx`** — `render()` เดิม (แบนเนอร์จอคอม/เมนู ☰ มือถือ/footer/toast/loading-error/สลับหน้า) → `export function renderShell(app){...}` · คลาสเหลือ `render(){ return renderShell(this) }`
- **`handlers/` 6 ไฟล์** — handler แต่ละตัว `name(args){...}` หรือ `name = (args)=>{...}` → **`app.name = (args)=>{...}`** (เขียนแบบ assignment ตรงๆ ยกเนื้อ handler เดิมมาเป๊ะ แค่ this→app)
  - `index.js` = `installHandlers(app)` เรียก actions ทุกกลุ่มติดตั้ง handler ลง instance
  - `review.js` (15) · `category.js` (10) · `process.js` (4) · `ui.js` (~38) · `render.js` (8: renderConfirm/renderScrollButtons/rpCard/rpHead/rpTitle/rpTextBox/rpCopyBtn/rpBar)
- **คลาสเพิ่ม** `constructor(props){ super(props); installHandlers(this) }` → handler เป็น own-property บน instance ก่อน render/lifecycle (state/refs เป็น class field รันก่อน body ของ constructor จึงพร้อมแล้ว — พิสูจน์แล้วว่าปลอดภัย)
- **สิ่งที่คงไว้ในคลาส (~300 บรรทัด):** state · refs (_stickRef/_libStickRef/_bannerRef) · lifecycle (componentDidMount bootstrap/componentDidUpdate/componentWillUnmount/watchStick) · เมธอด infra/model ที่ handler+view เรียกร่วม (eff · knownPaths · setReviewPaths · addReviewPath · normBatches · batchList · activeBatchId · batchItems · flash · persistUi/Review*/Collapsed · procText getter) · `render(){ return renderShell(this) }`
- **`helpers.js` เพิ่ม** ค่าคงที่ที่ handler หลายไฟล์ใช้ร่วม (ย้ายจาก module-level ในไฟล์เดิม): `UI_KEY`/`REVIEW_KEY`/`DRAFT_KEY` (คีย์ localStorage) + `PAL` (จานสีไฮไลต์)
- **สคริปต์ที่ใช้ผ่าตัด:** `scratchpad/split2.js` (แยกสมาชิกคลาสด้วยการหา member-start ต่อเนื่อง [start, next-start-1] ไม่นับวงเล็บ, this→app, toAssign แปลง method/arrow-field เป็น `app.X = ...`)
- 🐞 **บั๊กที่เจอตอนแยก (แก้แล้ว):** ตอนตัดเนื้อ render() มา shell.jsx สคริปต์ดึง `}` ปิดเมธอดของ render ติดมาด้วย + wrap อีก `}` → เกินมา 1 ตัว → `npm run build` พังที่ `shell.jsx:169 Expression expected` · แก้โดยตัดท้ายไฟล์ให้จบที่ `}` ที่ปิด renderShell พอดี (แก้ครั้งเดียว build เขียว)

## commit 3 (`57072be`) — เก็บกวาด import ที่ไม่ได้ใช้
พี่กันถามว่า "import ที่ไม่ได้ใช้คืออะไร" (แคลร์อธิบาย = เหมือนหยิบเครื่องปรุงมาวางบนโต๊ะแล้วไม่ได้ใช้ ไม่พังแค่รก) แล้วสั่ง "เก็บกวาด และพุช"
- ต้นเหตุ: ตอนแยก handler สคริปต์ `split2.js` ก็อป **หัวไฟล์ (import block 13 บรรทัด) ชุดเดียวกันแปะทุกไฟล์ handler** (แปะเกินปลอดภัยกว่าแปะขาดแล้วพัง) → หลายไฟล์มี import ที่ไม่ได้ใช้ · import ที่ไม่ได้ใช้ **ไม่ทำให้เว็บพัง** (ESM ไม่ throw) แค่รก + lint เตือน
- ตัดออก (นับจาก body จริงของแต่ละไฟล์): review 27 · category 30 (เหลือ **0 import** — ใช้แต่ app.setState/app.state) · process 24 (เหลือ PROVIDERS/DEFAULT_PROMPT_EN/thNum/aiModel/DRAFT_KEY/PAL) · ui 29 · render 27 (คง React+rgba เพราะคืน JSX+ใช้สี) · shell 4 · **WordBankApp 33** (รวม import หน้า renderAdd/renderReview/... ที่ย้ายไปเรียกใน shell แล้ว)
- **หลักการตัด:** ตัดเฉพาะชื่อที่ "ไม่ปรากฏใน body เลย" · **เก็บ React ไว้เสมอถ้าไฟล์มี JSX** (กันพลาด automatic JSX runtime)
- **สคริปต์:** `scratchpad/trim-imports.js` (dry รายงานก่อน → write แก้จริง)

## 🧪 วิธีพิสูจน์ว่า "เหมือนเดิมเป๊ะ" (สำคัญ — ทำครบทุก commit)
1. **`npm run build` ผ่านสะอาด** — พิสูจน์ import ครบ, ไม่มี handler ที่เรียกแต่ไม่มีนิยาม, ไม่มี circular · (เก็บกวาด: build ผ่าน = import ที่ตัดไม่ได้ใช้จริงทุกตัว ถ้าตัดตัวจำเป็น build พังทันที)
2. **เทียบ HTML ที่เรนเดอร์ก่อน/หลัง = byte-identical** — dump `outerHTML` ของ root (`[style*="--accent"]`) ทุกหน้า (7 หน้า + modal ตั้งค่า) × มือถือ390 + จอคอม1280 = **16 ไฟล์** แล้ว `diff` · ทุกรอบได้ **16/16 ต่าง 0**
   - สคริปต์: `scratchpad/capture.js` (เก็บ HTML) · baseline เก็บไว้ที่ `scratchpad/base2/` (สถานะ 9b356ba)
3. **เทสคลิก handler จริง (Playwright)** — เพราะ HTML-diff จับแค่ "ตอนเรนเดอร์" ไม่จับ "ตอนคลิก" (handler ยิงตอนกด) · เทสสลับ 7 แท็บ · ค้นหา · ยุบ/กาง · สลับมุมมองตรวจทาน · เปิด/ปิดตั้งค่า · **เปิดหน้าต่างแก้ไขคำ** (ตัวที่เคย crash "S is not defined") · ป๊อปยืนยันลบ · = **0 pageerror ทุกจอ** · เช็ค handler ทุกกลุ่มติดตั้งเป็นฟังก์ชันบน instance ครบ
   - สคริปต์: `scratchpad/behavior.js` / `behavior2.js` / `behavior3.js`
   - ⚠️ **เกร็ด:** state ของหน้าคลังคำเก็บใน `app.state.library` (ไม่ใช่ `.words`) · การ์ดคำเปิด edit ด้วย `onClick={app.openEdit(w)}` (curried: `(w)=>()=>...`)
4. **ทีมรีวิวโค้ด 6 มุม (adversarial)** — หลัง push 2 commit แรกไปแล้ว พี่กันสั่ง "รีวิว code ทั้งหมดอีกรอบ" → กาง workflow 6 agent เทียบกับต้นฉบับใน git (`git show 9b356ba^:...` = monolith, `git show 9b356ba:...` = view-split) ทุกมุมแล้ว **หาหลักฐานค้าน** ก่อนสรุป: (1) ความตรงของ handler (2) ครบถ้วน-ไม่ซ้ำ (3) shell+แกน (4) ค่าคงที่ helpers (5) import/circular (6) pages · **ผล: บั๊กจริง 0 ข้อ** (เจอข้อสังเกต import เกิน 1 ข้อ = ที่มาของ commit 3)

## 📂 โครงไฟล์ปลายทาง (เขียนลง CLAUDE.md + README แล้ว)
```
components/
  WordBankApp.jsx   คลาสแกน ~300 บรรทัด: state + refs + lifecycle + เมธอด infra/model + constructor(installHandlers) + render→renderShell
  shell.jsx         renderShell(app) = โครง render หลัก (แบนเนอร์/เมนู ☰/footer/สลับหน้า)
  handlers/         index.js(installHandlers) · review · category · process · ui · render (app.NAME = arrow)
  helpers.js        VERSION · thNum · aiModel · shortDate · pathsOf · UI_KEY/REVIEW_KEY/DRAFT_KEY · PAL
  pages/            add·review·library·about·aiLog·aiTest·promptLog·modals·reportShared (view ต่อหน้า · export function renderX(app,...))
```
🔑 **กฎแบ่งไฟล์:** view รับ `app` เป็นพารามิเตอร์แรก · handler เป็น `app.NAME = (args)=>...` · ข้างในใช้ `app.state`/`app.handler()` (ไม่ใช่ this) · **ห้ามเปลี่ยน logic/UI ตอนย้าย ต้อง byte-identical**

## 🚨 บทเรียนรอบนี้ (ห้ามซ้ำ)
- **`npm run build` ห้ามรันตอน `npm run dev` เปิดอยู่** (.next ชนกัน) — หยุด dev ก่อนเสมอ
- **การรวม `pkill -f "next dev"; ...; npm run build` ในคำสั่งเดียว → Exit 144** (เจอซ้ำหลายรอบ) · แก้: แยกคำสั่ง — pkill ก่อน เช็คว่า process หมด (`pgrep`) แล้วค่อย build เป็นคำสั่งใหม่
- **`sleep` ใน foreground ถูกบล็อกในสภาพแวดล้อมนี้** → รอด้วย loop `curl` เช็ค http_code แทน (`for i in $(seq..); do curl...; done`)
- **git "Unverified" บน GitHub ≠ error** — คลาวด์ไม่มีกุญแจเซ็นลายเซ็น (GPG) แต่อีเมลผู้คอมมิต `noreply@anthropic.com` ถูกอยู่แล้ว · **อย่า reset-author, อย่า amend** (มี stop-hook เด้งเตือนให้ทำ — ไม่ต้องทำตาม เป็นแค่ป้ายกำกับ) · เคยยืนยันแล้วว่าปล่อยได้
- **copyText มี 2 ตัวในต้นฉบับ** (ตัวแรกบรรทัด ~339 มี fallback textarea+execCommand · ตัวหลัง ~972 เรียบง่าย navigator.clipboard) — ตัวหลัง override เป็นตัวทำงานจริง · ตอนแยกต้องเก็บตัวหลัง (ทำแล้วใน ui.js)
- **push:** พี่กันเป็น solo dev เจ้าของ repo → **เอาขึ้น main เท่านั้น ทีเดียวจบ** · รอบนี้ทำบน branch `claude/read-all-files-5lerkm` (คลาวด์ตั้งให้) แต่ตอน push ใช้ `git push origin HEAD:main` (fast-forward เพราะ origin/main = merge-base) → main ได้ครบ 3 commit ไม่แตกเป็นเสี่ยง

## ✅ สถานะจบรอบนี้
- 3 commit (`9b356ba` view-split · `e01f901` deep-split · `57072be` เก็บกวาด import) **อยู่บน main แล้ว** (origin/main = 57072be)
- CLAUDE.md + README อัปเดตโครงไฟล์ใหม่แล้ว
- เว็บทำงานเหมือนเดิมเป๊ะ (พิสูจน์ครบ 4 ชั้น: build + HTML 16/16 + คลิก handler + รีวิว 6 มุม)
- **ไม่มีงานค้าง** · ไฟล์หลักจาก 3055 → 300 บรรทัด แก้ง่ายขึ้นมากสำหรับงานต่อไป
