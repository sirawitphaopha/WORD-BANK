# SESSION 2026-07-18 — คลังคำ (Word Bank)

3 push ในวันเดียว: **bf519d2** (คำตรวจทานขึ้นคลาวด์ + ประวัติ AI) → **dcbdda7** (ฝังฟอนต์ + ไฮไลต์คำที่แบ่ง) → **d8f430d** (ต่อ GPT API + prompt persona)

---

## push ที่ 1 — commit bf519d2

### 🖥 หน้าโหลด "AI กำลังจัดคำ" ใหม่ (งานค้างข้อ A จากเมื่อวาน)
พี่กันกลัวงานหายตอนรุ่น Pro ทำงานช้า
- เต็มจอกันคลิกทะลุ + นับวินาทีที่ใช้ + โชว์เจ้า/รุ่นที่กำลังใช้ (พร้อมโลโก้) + เตือน "อย่าปิดหรือรีเฟรช"
- ปุ่มยกเลิก (AbortController) · เซฟข้อความที่พิมพ์ไว้ใน localStorage → เผลอรีเฟรชระหว่างรอ ข้อความไม่หาย

### 📖 มุมมองใหม่ 2 แบบ (รวมเป็น 5 มุมมองในหน้าตรวจทาน)
- **✂ จับกลุ่มประโยค** — AI แนบ `source` (ประโยคต้นทาง) มากับคำที่สกัด → จัดกลุ่มคำใต้ประโยคแม่
- **📖 แบบคลังคำ** — จัดคำตามหมวด→หมวดย่อยเป็นต้นไม้ยุบขยาย (งานค้างข้อ B)

### ☁️ คำตรวจทานขึ้นคลาวด์ (`wb_review`)
- คลาวด์เป็นหลัก + localStorage สำรอง · sync ทุก mutation ผ่าน `persistReview → scheduleReviewPush` (หน่วง 0.5 วิ)
- flush ตอนปิดหน้าด้วย sendBeacon
- ⚠️ **จุดนี้แหละที่ฝังระเบิดเวลาไว้** — ใช้ `keepalive` ซึ่งจำกัด 64KB (ระเบิดจริงวันที่ 19)

### 📈 ประวัติการใช้ AI — เก็บทุกอย่าง (`wb_ai_log`)
ทุกครั้งที่เรียก AI บันทึก: เจ้า/รุ่น/เรื่อง/ข้อความเข้า/จำนวนคำ/คำที่สกัด/คำที่บันทึกจริง/token เข้า-ออก/**ค่าใช้จ่ายประมาณ**/เวลาที่ใช้/สำเร็จ-พลาด
- `lib/pricing.js` = ราคาต่อ 1M token ต่อรุ่น + `estCost()`
- หน้าที่ 4 "ประวัติ AI" — สรุปยอดรวม + การ์ดต่อเจ้า + รายการ 200 ล่าสุด + กรองรายเจ้า

### 🎨 อื่น ๆ
- ป้าย "มีในคลังแล้ว" + ปุ่มลบคำซ้ำ **แดงกระพริบ** (keyframes `wbalert`)
- **โลโก้จริงของ AI ทุกเจ้า** `lib/brandIcons.jsx` (SVG แท้) — พี่กันท้วงว่า *"icon AI มันต้อง icon จริงสิ"* หลังแคลร์ใส่ emoji ไปก่อน
- ป๊อปเตือนใหญ่ตอน AI ล้มเหลว (`alert:true` = ปุ่มเดียว ไอคอนใหญ่) บอกสาเหตุจริง ข้อความยังอยู่ครบ
- เอาปุ่ม "ทิ้งทั้งหมด" ออก (ซ้ำซ้อนกับเลือกทั้งหมด→ลบ และอันตราย)

---

## push ที่ 2 — commit dcbdda7 (งาน UX/ดีไซน์ยกใหญ่)

### 🔤 ฟอนต์ฝังในเว็บ (ตั้งเป็นกฎโปรเจกต์)
- เปลี่ยนจาก `<link>` Google CDN → **next/font/google** (`app/layout.js`) · แทนชื่อฟอนต์ 48 จุดในโค้ดเป็น `var(--font-xxx)`
- 🚨 **กฎใหม่: ฟอนต์ใหม่ทุกตัวต้องฝังแบบนี้เสมอ ห้ามใช้ CDN** (ขึ้นชัวร์ทุกเครื่อง/เน็ต ไม่กระพริบ)

### 🖍 ระบบไฮไลต์คำที่ AI ดึงจากประโยค
- `pulledFrom` (map ประโยค → คำที่ดึง) · `renderWord(r, style, editable)` = ตัวช่วยกลางแสดงคำหลักทุกมุมมอง
- ประโยคตั้งต้นที่มีคำถูกดึง → ไฮไลต์คำนั้นในตัวคำหลัก (กดเพื่อแก้ผ่าน state `editCard`)
- `highlightWords` (สีเดียว) / `highlightColored` (หลายสี — คำแรกเหลืองเหมือนหน้าอื่น คำถัดไปสีอื่น)

### 🔧 บั๊กเส้นประหาย (บทเรียนเรื่องลำดับ key)
```js
{...baseStyle, border:'none', borderBottom: X}   // ❌ key borderBottom อยู่ก่อน border:none เลยโดนลบ
const {borderBottom: bb, ...rest} = baseStyle;   // ✅ ดึงออกก่อน แล้วใส่ทีหลัง
```
พี่กันท้วง 2 รอบว่าเส้นประหาย และสั่งให้ไปดูในโครมเอง — เช็คด้วย screenshot จริงถึงเจอ

### 🃏 การ์ดจัดตรงกัน + ตาราง/คอลัมน์
- จองพื้นที่ป้ายเท่ากันทุกการ์ด · คำหลัก div/input สูงเท่ากัน · หมวดย่อยชิดล่างสุด (`marginTop:auto`)
- ตาราง: คอลัมน์ "สถานะ" แยก กดเรียงได้ · คอลัมน์: 8 หมวดแนวนอนแถวเดียว เต็มจอ
- แถบเครื่องมือตรึง (sticky) · ป้ายหมวดเว้นที่ให้ ▾ + ชื่อยาวตัด …

---

## push ที่ 3 — commit d8f430d (ต่อ GPT API)

### 🔧 แก้บั๊ก temperature — GPT-5.x ใช้งานไม่ได้
- ต้นเหตุ: `callOpenAICompatible` ส่ง `temperature: 0.2` เสมอ แต่ GPT-5.x (reasoning) รับได้แค่ค่า default 1 → error 400
- แก้: ส่ง temperature เฉพาะ model ที่ไม่ขึ้นต้นด้วย `gpt-5`

### 🔢 เทสรุ่น GPT ทุกตัวจริง
```
GPT-5.1 = 5 คำ (เยอะสุด) · Gemini = 4 · GPT-5.5/4.1 = 3 · GPT-5.6 (ทั้ง 3 ตัว)/5.4 = 0-1
```
⚠️ **บทเรียน**: แคลร์เทส 5.6 ตัวเดียวแล้วสรุปว่า "5.x ใช้ไม่ได้" → **พี่กันท้วงว่าผิดตรรกะ** ต้องเทสให้ครบก่อนสรุป
ผลจริงคือ reasoning model บางตัวสกัดไม่ดี **แต่ไม่ใช่ทุกตัว** (5.1/5.5 ดี)

### 📝 prompt — สวมบทผู้เชี่ยวชาญ
- persona: นักภาษาศาสตร์/ผู้เชี่ยวชาญวรรณกรรมไทยระดับสูง (ช่วยเรื่องความแม่นในการจัดหมวด)
- บังคับสกัดคำเยอะไว้ก่อน เพราะ**ผู้ใช้เป็นคนตรวจทานลบเอง** (พี่กันย้ำ: *"สกัดออกมาเยอะ แต่อย่าลืม เราเป็นคนตรวจทาน ถ้าไม่ชอบก็ลบ"*)

### ⬇ ปุ่มส่งออก + 🔗 ปุ่มขอกุญแจ
- ส่งออกผลตรวจทานเป็น .txt (คำ/ชนิด/หมวด/หมวดย่อย/ต้นฉบับ/ความหมาย/ที่มา) ไว้เทียบผลข้ามโมเดล
- ปุ่ม "ขอกุญแจ" ทุกเจ้า ลิงก์ไปหน้าขอ API key ของเจ้านั้น

### 💳 เรื่องการเงิน (บันทึกไว้เป็นบทเรียน)
- ตั้งใจต่อ Claude API ก่อน แต่**บัตรเครดิตถูกปฏิเสธ 2 ใบ** (Payment failed, ตัดเงิน $0) วินิจฉัยว่าธนาคารบล็อก
- ข้ามไป OpenAI → บัตรผ่าน เติม $10 ใช้งานได้
- 🔑 กฎที่ย้ำ: **ห้ามเอากุญแจ API มาวางในแชท** ใส่เองในไฟล์ `.env.local` เท่านั้น · ข้อมูลการเงินผู้ใช้ต้องกรอกเอง 100%

---

## ⚠️ บทเรียนของวันนี้
- **เทสไม่ครบแล้วสรุป = ผิดตรรกะ** (เรื่องรุ่น GPT)
- **ลำดับ key ใน object spread มีผลจริง** — `border:'none'` ลบ `borderBottom` ที่ประกาศไว้ก่อนหน้า
- **พี่กันสั่งให้เช็คในเบราว์เซอร์จริงเอง** อย่าเชื่อแค่โค้ดที่เขียน

## ค้างไว้ (ต่อวันที่ 19)
- 🐛 **BUG: จัดคำรอบใหม่ทับคำที่ยังตรวจไม่เสร็จ** — `process()` ทำ setState แทนที่ทั้งชุด ควร append + dedupe
- C. Ensemble (รัน 2 เจ้าแล้วรวมผล) · D. prompt แยกต่อเจ้า · E. ระบบวัดความแม่นการจัดหมวด
- แปะชนิดคำให้ 679 คำเก่า · ต่อ API เจ้าที่เหลือ
