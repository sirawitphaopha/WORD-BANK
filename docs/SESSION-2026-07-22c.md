# SESSION 2026-07-22c — ดึงสำเนาคลังเป็นไฟล์ + ตรวจทานเทียบต้นฉบับ .docx (multi-agent) + เพิ่ม/แก้ 6 คำ + idea Q

> ต่อจาก SESSION-2026-07-22b · commit รอบนี้ = `1aeeeef` (push แล้วบน main) · โปรเจกต์ = **คลังคำ (Word Bank)** · ผู้ใช้ = **พี่กัน** · ผู้ช่วย = **แคลร์** · รันในเครื่องพี่กัน (มีกุญแจ Supabase + AI จริงใน `.env.local`)

## งานหลักรอบนี้

### 1. 📄 ดึงคลังคำทั้งหมดเป็นไฟล์ `docs/library-export.json`
- **พี่กันสั่ง:** เอาคลังทั้งหมดจาก Supabase มาเก็บเป็นไฟล์ใน repo · **เหตุผล: Claude บนคลาวด์เข้า Supabase ไม่ค่อยได้** → ให้อ่านคลังจากไฟล์นี้แทน · ไว้ทำงานใหญ่ **"ไล่ดูทุกคำ + สร้างหมวดใหม่ยกชุด"** (แผนของพี่กัน)
- ไฟล์มี: `_meta` (จำนวน+หมายเหตุ) · `categories` (9 หมวด) · `branches` (143 กิ่ง) · `words` (ครบทุก field) · `tree` (จัดหมวด→กิ่ง→คำ)
- ดึงผ่าน Supabase REST (`/rest/v1/wb_*?select=*`) ด้วย service key + แบ่งหน้า (Range header) · **เป็นสแนปช็อต ณ วันดึง** — คลังเปลี่ยนแล้ว re-export ทับใหม่ได้ (สคริปต์อยู่ scratchpad, ทำซ้ำง่าย)

### 2. 🔍 ตรวจทานคลังเทียบต้นฉบับ `C:\Users\PKH\Downloads\คำศัพท์.docx` (multi-agent) → `docs/library-audit-vs-source.md`
- **พี่กันสั่ง:** เทียบว่านำเข้าจากต้นฉบับครบไหม + "วิเคราะแบบ multi agent"
- **วิธี:** แกะข้อความจาก .docx (`word/document.xml` ผ่าน PowerShell `System.IO.Compression`) → แบ่ง 8 หมวด → **ปล่อย 8 agent (general-purpose · sonnet) ขนานกัน หมวดละตัว** อ่านไฟล์รายหมวดที่แยกไว้ + จับคู่กับคลังแบบยืดหยุ่น → รายงานคำที่ตกหล่น
- **🚨🚨 บทเรียนใหญ่ (พลาด 2 รอบก่อนเจอ · ห้ามซ้ำ):** ตอนแกะ .docx เป็น "คำ" **อย่าตัดบรรทัดที่ `ilvl==='ไม่ใช่list'` (ไม่มี `<w:numPr>`) ทิ้ง** — Word เก็บ**ลิสต์คำหลายชุดเป็นย่อหน้าธรรมดา** (ไม่ได้ทำเป็น bullet) เช่น `โอบอุ้ม, ฉุดลาก, ดุนหลัง, หามศพ, เหวี่ยง` · ถ้าตัดทิ้งจะนับคำ**ขาดเยอะ**
  - แคลร์พลาด: parser แรกนับได้ **539 คำ** แล้วสรุปผิดว่า "137 คำถูกเพิ่มทีหลังผ่านเว็บ" · **พี่กันท้วงว่าไม่เคยเพิ่มผ่านเว็บ** → กลับไปเช็ค XML ดิบ พบว่าคำพวกนั้น**อยู่ในต้นฉบับหมด** · parser แค่แกะขาด (ดรอปบรรทัดไม่ใช่ bullet)
  - **แก้ parser (รวมบรรทัดลิสต์ที่ไม่ใช่ bullet):** ได้ **689 คำ** (หมวด 3: 255 ≈ คลัง 247) → **ต้นฉบับ ~689 ≈ คลัง 680 = นำเข้าซื่อตรง**
- **การจับคู่อัตโนมัติเชื่อ 100% ไม่ได้** (ตัดวงเล็บกลางคำ=ไฮไลต์ ทำคำเพี้ยน · จับคู่หลวมไปคลอนแคลนหลุด) → **ยึดผลจากการค้นตรงในคลัง (`.text===w` / `.includes`)** เป็นหลัก
- Supabase counts ตอนตรวจ: `wb_words` 676 · `wb_categories` 9 · `wb_branches` 143 · `wb_novels` 0 (ว่าง · novel เก็บรายคำใน wb_words) · `wb_review` 0

### 3. ✏️ เพิ่ม/แก้ 6 คำในคลัง (676 → 680)
คำที่ค้นตรงแล้วยืนยันว่ามีปัญหา (พี่กันสั่ง "เอาเข้าเลย · 1 ก่อน"):
- **เพิ่ม 4 คำ** (insert `wb_words` ผ่าน REST · กิ่งมีอยู่แล้วไม่ต้องสร้างใหม่):
  - `บอกใบ้` → c2 (หมวด 3) · กิ่ง "การสื่อสารและพฤติกรรมทางสังคม / การพูดคุย/บอกเล่า / เจตนาและเนื้อหา" · kind word
  - `วิงวอนขอความช่วยเหลือ` → c4 (หมวด 5) · กิ่ง "ใจปั่นป่วน - ทุกข์ร้อน / เศร้า/สิ้นหวัง/วิงวอน" · kind phrase
  - `ล่อกแล่ก` → c7 (หมวด 8) · กิ่ง "นิสัยขี้กลัว/ไม่มั่นคง" · kind word
  - `ความกลัวระคนสงสัย` → c5 (หมวด 6) · กิ่ง "อาการและทิศทางของอารมณ์ / จังหวะที่เริ่มรู้สึกฯ" · kind phrase
- **แก้ 2 คำ** (PATCH):
  - id `e65ea9fc-30ff-4a1a-943e-a010ded02cb5`: `"จิตใจไม่มั่นคง"` → **`คลอนแคลน`** (meaning=`จิตใจไม่มั่นคง`) — เดิมเก็บความหมายแทนคำ
  - id `a5849e25-a507-4b80-919c-b39c26da01a2`: `"ระคน ความกลัวระคนสงสัยรู้สึกปนกัน"` → **`ระคน`** — เดิมมัดคำ+บริบทรวมกัน · แยก "ความกลัวระคนสงสัย" ออกเป็นคำต่างหาก (ข้อเพิ่มด้านบน)
- ทำผ่าน **Supabase REST + service key** (ไม่ใช่ SQL Editor · insert/patch ตรง) · `reviewed:true` `novel:null` `subpaths:[subpath]` · id ใช้ `crypto.randomUUID()`
- re-export `library-export.json` เป็น 680 คำ ยืนยันครบทั้ง 6

### 4. 🏷 idea Q — ติด "แท็กที่มา" ให้ทุกคำ (พี่กันเสนอ · เขียนเก็บ idea.md + roadmap)
- ทุกคำที่เข้า `wb_words` ควรเก็บ: **วันที่ · AI เจ้าไหน/รุ่นไหน · คำดิบ(พิมพ์เอง) vs วลีสกัดจากประโยค** → รู้ลำดับก่อน-หลัง + กรองตามที่มาได้
- เหตุ: ตรวจทานเทียบต้นฉบับแล้ว **แยกไม่ออกว่าคำไหนมาจาก docx / มาจากการสกัด** → งง
- สถานะ: `wb_words` มี `created_at`+`kind` แล้ว · **ยังไม่มี** ai_provider + from_extract · ข้อมูลนี้มีใน `wb_review` (batch_ai/source/batch_at) แต่ตอน `_save` review→words **ไม่ได้ copy มา** · ต้องเพิ่มคอลัมน์ + แก้ `_save` (`handlers/review.js`) + ตัวกรองหน้าคลัง

## บทเรียน (ห้ามซ้ำ)
- **แกะ .docx: อย่าตัดบรรทัดไม่ใช่ bullet ทิ้ง** (Word เก็บลิสต์คำเป็นย่อหน้าธรรมดาเยอะ) — เช็คด้วยว่า text ที่แกะได้ ≈ ความยาว text จาก XML ดิบ (18,471 ≈ 18,777 ตัวอักษร = ครบ · แต่ตอนแตกเป็นคำต่างหากที่พลาด)
- **พี่กันท้วง = เชื่อ + กลับไปเช็คของจริงทันที** (ครั้งนี้ท้วงว่าไม่เคยเพิ่มคำผ่านเว็บ → ถูก แคลร์สรุปผิดเอง)
- **การจับคู่คำแบบ fuzzy เชื่อ 100% ไม่ได้** — ยึดค้นตรง (`.text===` / `.includes`) เป็นหลักในการฟันธงว่าคำหาย
- multi-agent เหมาะกับงานเทียบ/ตรวจแบบนี้ (8 agent ขนาน หมวดละตัว · อ่านไฟล์เอง ไม่ยัด context แคลร์) แต่ยังต้อง verify ด้วยการค้นตรง

## งานค้าง (ทำต่อ)
1. 🔬 **ไล่ดูลึกรายหมวดกับพี่กัน** — พี่กันจะพาดูทีละหมวด หา "จุดที่กรอง/จัดผิด" (มีไฟล์ library-export.json 680 คำในมือแล้ว) · น่าจะเจอคำเพี้ยน/จัดผิดเพิ่ม (โดยเฉพาะกลุ่มการสื่อสาร/การพูดคุย หมวด 3 ที่เดิมโดนดรอป)
2. 🏷 **idea Q (แท็กที่มา)** — เพิ่มคอลัมน์ provenance ใน wb_words + copy ตอน _save + ตัวกรอง
3. (จากก่อนหน้า) Grok/Muse Spark probe · เทสโมเดลเดี่ยว · M2 multi-agent · เวอร์ชัน ๓ เทส AI
