# SESSION 2026-07-17 — คลังคำ (Word Bank) วันแรก

สร้างโปรเจกต์ใหม่ทั้งอันจากไฟล์ดีไซน์ handoff → push 5 รอบ → deploy ขึ้นเว็บจริงสำเร็จ
commit: `08df92d` (โปรเจกต์ทั้งอัน) → skills → `c7acd5d` (ระบบ AI หลายเจ้า) → `bf0b24b` (deploy Cloudflare) → `1c590ca` (UX ตรวจทาน + ป๊อปยืนยันกลาง)

---

## โปรเจกต์นี้คืออะไร
เว็บส่วนตัวสำหรับนักเขียนนิยาย เก็บคำงาม/วลี/ประโยคที่เจอตอนอ่าน ให้ระบบช่วยแก้สะกด แยกวลี จัดหมวดอัตโนมัติ
ธีมกระดาษวรรณกรรม (ครีม + หมึกน้ำตาล + ฟอนต์ลายมือ Charmonman)

## เทคโนโลยีที่เลือก
- **Next.js 15 (App Router) + React 19** — โค้ดเป็น JavaScript ไม่ใช่ TypeScript
- **Supabase** ฝากในโปรเจกต์ tb-calculator ตารางขึ้นต้น `wb_` (free tier เต็ม 2 โปรเจกต์แล้ว)
- **ไม่มีล็อกอิน** · RLS deny-all · เข้าฐานข้อมูลผ่าน API route (service_role) เท่านั้น เบราว์เซอร์ไม่มีกุญแจเลย
- ตัวจัดคำเริ่มจาก heuristic (`lib/engine.js`) ออกแบบให้สลับเป็น AI จริงได้ทีหลังโดยไม่แตะ UI

## ฐานข้อมูล
`wb_categories` (c0..c8) · `wb_novels` · `wb_subcategories` · `wb_words`
- `subpath` = เส้นทางหมวดย่อยลึก 3 ชั้น คั่นด้วย " / " → หน้าคลังคำสร้างต้นไม้จากค่านี้
- `highlight` = คำเน้นที่เดิมอยู่ในวงเล็บ (เก็บไว้เผื่อทำระบบไฮไลต์)
- นำเข้าคลังจริง **679 คำ** จากไฟล์ .docx ของพี่กัน ผ่าน `scripts/import_tree.mjs`

## สิ่งที่ทำเสร็จวันแรก
- 3 หน้า: เพิ่มคำ / ตรวจทาน (การ์ด-ตาราง-คอลัมน์ลากวาง) / คลังคำ
- คลังคำ = **ต้นไม้หมวดย่อย 3 ชั้น ยุบขยายได้ + สารบัญกดกระโดด** · เปิดมายุบไว้ก่อน (กันหน่วง 679 การ์ด)
- **สกิลติดไปกับ repo** `.claude/skills/working-with-gun` (กฎกลาง) + `.claude/skills/wordbank` (เฉพาะโปรเจกต์)
  เหตุผล: เผื่อสั่งงาน Claude บนคลาวด์ ตัวนั้นจะได้อ่านกฎเหมือนกัน

## 🧠 ระบบ AI สลับหลายเจ้า (commit c7acd5d)
- **8 ตัวเลือก**: พื้นฐาน (heuristic ฟรี) + Gemini / Claude / GPT / DeepSeek / Kimi / Qwen / GLM
- GPT/DeepSeek/Kimi/Qwen/GLM ใช้ตัวแปลงเดียวกัน (`callOpenAICompatible`) · Gemini/Claude แยก
- ไฟล์: `lib/ai.js` (หัวใจ) · `lib/providers.js` (ทะเบียนเจ้า+รุ่น) · `lib/prompt.js` (prompt EN+TH)
- **prompt แก้บนหน้าเว็บได้** 2 กรอบ อังกฤษ (ส่งจริง) / ไทย (ไว้อ่าน) · ยุบไว้ + ต้องกด "เปิดแก้ไข" ก่อน
- **ชนิดคำ kind** (word/phrase/sentence) — คอลัมน์ใหม่ กดเปลี่ยนได้ กรองได้
- เตือนคำซ้ำ + ตอนบันทึกข้ามคำที่มีในคลังแล้ว

## ☁️ Deploy ขึ้น Cloudflare Workers (commit bf0b24b)
- ใช้ **@opennextjs/cloudflare** (Next.js มี server → ต้องใช้ Workers ไม่ใช่ Pages)
- 🚨🚨 **บทเรียนสำคัญที่สุดของวัน: env vars มี 2 ที่แยกกัน**
  - "Build → Variables and secrets" = ใช้ตอน**สร้าง**เว็บ
  - โค้ดอ่าน `process.env` ตอน **runtime** → ต้องใส่ที่ **Settings → Variables and Secrets** ต่างหาก
  - ใส่ผิดที่ = เว็บหากุญแจไม่เจอ (bootstrap error) ทั้งที่ build ผ่าน

## 🎛 UX ตรวจทาน + ป๊อปยืนยันกลาง (commit 1c590ca)
- เอา **sidebar ออก** ใช้แบนเนอร์บนเสมอ
- 🛡 **ระบบยืนยันกลาง** `askConfirm({title,msg,okLabel,danger,onOk})` + `renderConfirm()` (คลิกนอกป๊อปไม่ปิด)
  → **ปุ่ม action ใหม่ทุกปุ่มต้องผ่านตัวนี้เสมอ**
- ปุ่ม 📋 วาง (จากคลิปบอร์ด) · ชื่อนิยายเป็นดรอปดาวน์เลือกเรื่องเก่าได้
- ป้าย **✂ แยกจากประโยค** (prompt สั่ง AI แปะโน้ต) · ปุ่ม ⚠ ลบคำซ้ำ · ตารางกดหัวคอลัมน์เรียงได้

## ⚠️ บทเรียนที่ห้ามซ้ำ
- 🚨 **ห้ามรัน `npm run build` ขณะ `npm run dev` เปิดอยู่** → `.next` ชนกัน (Cannot find module ./xxx.js) ต้องหยุด dev + ลบ `.next` ก่อน
- แกะ .docx: list ilvl 0/1/2 = หมวดย่อย 3 ชั้น (ครบกว่าแปลงเป็น .md ซึ่ง flatten แล้วคำหล่น)
- วงเล็บในคำศัพท์มีความหมายต่างกัน: `(แปล:X)` / เว้นวรรค `(X)` = ความหมาย · `คำ(X)` ติดกัน = รวมเป็นวลีเดียว + เก็บ highlight · `(เน้น...)` = คำบรรยายหัวข้อ · `(ใช้...)` = ทิ้ง
- 🚨 กฎที่ตั้งวันนี้: **ทุก push ต้องอัปเดต README + CLAUDE.md ให้ละเอียด** + commit message ละเอียดระดับ file-by-file

## ค้างไว้ (ต่อวันพรุ่งนี้)
- A. หน้าโหลด "AI กำลังจัดคำ" ใหม่ — กันงานหาย (เตือนอย่ารีเฟรช + นับวินาที + ปุ่มยกเลิก + เซฟข้อความชั่วคราว)
- B. มุมมองต้นไม้หมวดย่อยในหน้าตรวจทาน (พี่กันให้ความสำคัญกับหมวดย่อยมาก)
- ประวัติการใช้ AI · แปะชนิดคำให้ 679 คำเก่าย้อนหลัง
- ⚠️ พี่กันยังไม่ได้เทสหน้าจริงของระบบ AI (สั่งให้ push ก่อน ค่อยเทส)
