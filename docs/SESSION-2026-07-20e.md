# SESSION 2026-07-20e — ยกเครื่องหน้าคลังคำ (ตัวกรอง/การ์ด/ฟอนต์) + หน้าเกี่ยวกับ + ไอคอนยุบ-กางทั่วเว็บ

> เขียนให้ AI ที่ไม่เคยคุยกับพี่กันมาก่อนอ่านแล้วทำงานต่อได้ทันที (สมมติว่าไม่มีบริบทอะไรเลย)
> รอบนี้ทำบน **cloud agent** · รอบที่ 5 ของวัน 20 ก.ค. 2569 (ต่อจาก .md/-20b/-20c/-20d)
> **5 commit ขึ้น main แล้ว** (พี่กันพิมพ์ "พุชเลย")

## คนทำงาน (ทวนไว้ เผื่อ AI ตัวใหม่)
- **พี่กัน** = เจ้าของโปรเจกต์ · เภสัชกร ไม่มีพื้นฐานโค้ด แต่เข้าใจ logic ดี · อธิบายด้วยภาษาคน อย่าพ่นศัพท์เทคนิค · **ห้ามพูดรหัสหมวด c0–c8** เรียก "หมวด + เลข/ชื่อไทย" เท่านั้น
- **แคลร์** = ผู้ช่วย AI (ผู้หญิง ใช้ ค่ะ/นะคะ · แทนตัวว่า "แคลร์" เรียกผู้ใช้ว่า "พี่กัน")
- 🔑 **สไตล์การทำงานที่พี่กันชอบ (รอบนี้ยืนยันชัด):** สั่งงาน UI ทีละจุดเล็กๆ แล้วดูรูปจริง → ติชม→ แก้→ ดูใหม่ เป็นรอบๆ · **ทุกครั้งต้องเทส Playwright + เปิดรูปดูจริงก่อนบอกว่าเสร็จ** · แก้เฉพาะมือถือให้ guard `S.isMobile` เสมอ (จอคอม byte-identical) เว้นแต่สั่งชัดว่า "ทั้ง 2 จอ"

## 🎯 งานรอบนี้ (5 commit) — ทั้งหมดอยู่ใน `components/WordBankApp.jsx` + เอกสาร

### commit 1 (`8cabdeb`) — หน้าคลังคำ: ยกเครื่องแถบตัวกรอง
พี่กันติงว่าชิปหมวด 8 อันบนมือถือเรียงยาวกินจอ + ขอตรึงช่องค้นหา → แล้วกำหนด layout ใหม่เอง
- **มือถือ: แถบตัวกรองตรึง (sticky) จัด 3 แถวชิดๆ** (กันแบนเนอร์ตรึงสูงเกิน) — แถว 1 ค้นหา+ชนิดวลี+เรียง · แถว 2 เลือกนิยาย · แถว 3 เลือกหมวดวลี
  - เดิมมือถือ `position:static` (ไม่ตรึง) → เปลี่ยน `_libStickRef` เป็น `sticky` ทั้ง 2 จอ · `tocTop` มือถือจาก `bannerH+8` → `bannerH+libStickH+10` (เท่าจอคอม · หัวข้อหมวดหลบใต้แถบตรึงพอดี ไม่ทับ)
- **ชิปหมวด 8 อัน → ยุบเป็นดรอปดาวน์ตัวเดียว** (มือถือ) · **จอคอมคงชิปสีเหมือนเดิม** (พี่กันเลือกเอง ผ่าน AskUserQuestion — จอกว้างไม่กินที่ ชิปสวย)
- **เปลี่ยนชื่อดรอปดาวน์ทั้ง 2 จอ:** ชนิด "ทุกชนิด"→**"ชนิดวลี"** · เรียง "ล่าสุดก่อน"→**"เรียง"** · นิยาย "ทุกเรื่อง"→**"เลือกนิยาย"** · หมวดมือถือ "ทุกหมวด"→**"เลือกหมวดวลี"**
- 🌟 **ตัวกรองนิยายเลือกได้หลายเรื่อง (ทั้ง 2 จอ):** state `filterNovel` (string) → **`filterNovels` (array · ว่าง=ทุกเรื่อง)** + `novelMenuOpen` · handler `toggleNovelMenu/toggleNovel(n)/clearNovels` · UI = ปุ่ม custom `novelPicker(full)` เปิดแผงติ๊ก checkbox หลายเรื่อง + ปุ่ม "ล้าง (ดูทุกเรื่อง)" · label ปุ่ม = "เลือกนิยาย"(ว่าง)/ชื่อเรื่อง(1)/"เลือกนิยาย (N)"(หลาย) · `matchQN` = `selNovels.length===0 || selNovels.includes(w.novel)`

### commit 2 (`c4a09fa`) — การ์ดคำมือถือ + หน้าเกี่ยวกับ
- **การ์ดคำหน้าคลังคำ (มือถือ):** พี่กันติงว่าเล็ก-ใหญ่-สูงไม่เท่ากัน (วลียาวกลายเป็นการ์ดสูงแคบ) → **วลียาวกินหลายคอลัมน์**: `span = tlen>30?3 : tlen>15?2 : 1` แล้วใส่ `gridColumn:'span N'` (สั้น 1 · กลาง 2 · ยาว 3=เต็มแถว) · gridStyle เพิ่ม `gridAutoFlow:'dense'` (อุดช่องว่าง) + `alignItems:'stretch'` (สูงเท่ากันในแถว) · การ์ดเพิ่ม `minHeight:44px` + flex-column-center
- **หน้าเกี่ยวกับ (renderAbout · ทั้ง 2 จอ):**
  - `head(num,title)`: **เอาวงกลมเลขไทย ๑–๖ ออก** · ชื่อหัวข้อเปลี่ยนเป็น **สีแดงน้ำตาล `var(--accent,#9c3b2b)`** (สีที่วงกลมเลขเคยเป็น) · คง `num` ไว้เฉยๆ ไม่ใช้ (กันแก้ทุก call)
  - บล็อกเทคโนโลยี: ลบ `techs1/techs2/techChip` (ชิปพิลล์ + Claude Code แยกใต้เส้นแบ่ง) → `allTechs` 6 ตัว **รวม Claude Code** + `techCell` = **grid 2 คอลัมน์แบบบล็อกชนิด AI** (ไอคอน + ชื่อบน + คำไทยล่าง stack กันคำไทยล้น)

### commit 3 (`dfa94c9`) — ปุ่มเลือกฟอนต์บนมือถือ
พี่กันขอที่เปลี่ยนฟอนต์บนมือถือ (จอคอมมีปุ่ม segment 6 ตัวอยู่แล้ว) · รอบแรกทำ select 3 ตัว "ตัวหลัก" แต่พี่กันติง 2 จุด: (1) กรอบอวบกว่ากรอบนิยาย (2) "ตัวหลัก" ไม่สื่อ ให้เขียน "เลือกฟอนต์" + เอาฟอนต์ครบ → **ทำ `fontPicker` = ปุ่ม dropdown โครงเดียวกับ `novelPicker` เป๊ะ (กรอบสูงเท่ากัน 42px)** · โชว์ "เลือกฟอนต์" · เมนูฟอนต์ครบ **6 ตัว** (ไทรรงค์/สารบรรณ/Sarabun New/Cordia New/ไมตรี/ชลบุรี) แต่ละอันพรีวิวฟอนต์จริง · state `fontMenuOpen` · เขียน `wordFont` (key เดียวกับจอคอม · การ์ดใช้ `FF/fsz/FWEIGHT` อยู่แล้ว) · แทรกแถว 2 ข้าง novelPicker

### commit 4 (`c1fee83`) — ไอคอนยุบ/กางทั่วเว็บ (ทั้ง 2 จอ)
พี่กันเห็นปุ่มยุบ/กางมือถือที่ทำเป็นลูกศรในกรอบสีแล้วชอบ → สั่ง "แก้ให้เป็นแบบนี้ ทุกจุดในเว็บ ทั้งโมบายและเดส" (ยืนยัน "ทุกจุดจริงๆ" ผ่าน AskUserQuestion) · แล้วติงเพิ่ม 2 รอบ: กล่อง prompt ต้องมีกรอบเหมือนปุ่มขยาย + กรอบต้องใหญ่
- **กลุ่ม "กรอบสี (badge)"** = ไอคอนลูกศรในกรอบกลมสี (ตัวครีม+เงา): ปุ่มมือถือ "ยุบ/กางทุกหมวด" (22px) · ปุ่มจอคอมสารบัญ "ยุบ/ขยายทั้งหมด" (19px · เดิมตัวอักษรเปล่า) · **กล่อง "คำสั่ง AI (prompt)" หน้าเพิ่มคำ** (เดิมลูกศรเปล่า → กรอบน้ำตาล **ใหญ่ 34px** ลูกศรชั้นในหมุน 90° เมื่อเปิด) · ยุบ=▲น้ำตาล primary · กาง/ขยาย=▼แดงน้ำตาล accent
- **กลุ่ม "ลูกศรเปล่าเด่น"** (มีเป็นสิบๆ อัน ไม่ใส่กรอบกันแน่น) = สีน้ำตาลเข้ม primary + 13px + หนา 800: `chev()` หน้าหัวข้อหมวด/หมวดย่อยในต้นไม้ (เดิมจาง #b0a184 11px) · แถบหมวดหน้าตรวจทานมุมมอง "แบบคลังคำ"
- ไม่แตะ: ปุ่ม "ดูข้อความเต็ม" หน้าประวัติคำสั่ง (เป็นปุ่มมีสีอยู่แล้ว) · ลูกศร ▾ ของ dropdown เลือกนิยาย/ฟอนต์/เรื่อง (เป็นตัวชี้เมนู ไม่ใช่ยุบ-กาง)

### commit 5 (`e3c462b`) — README ให้ตรงงาน
ตัวกรองหน้าคลังคำ: เรื่อง → "เลือกนิยายได้หลายเรื่องพร้อมกัน" + มือถือแถบ 3 แถวตรึง + เลือกฟอนต์ได้

## 🧪 วิธีเทส (สำคัญ — คลาวด์ไม่มีกุญแจ DB)
- **Playwright route-mock** ยัด mock เข้า `/api/bootstrap`,`/api/logs`,`/api/review` (ไม่แตะโค้ดแอป) · สคริปต์+mock อยู่ใน scratchpad (`mockdata.js` = คำ 8 หมวด · เติมวลียาวใน c0 เพื่อเทส span)
- Chromium: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` · รัน `NODE_PATH=/home/user/WORD-BANK/node_modules node <script>` `--no-sandbox`
- นำทางมือถือ: กด `[aria-label="เมนู"]` (☰) → คลิก nav "คลังคำ"/"เกี่ยวกับ" · จอคอม: คลิกแท็บ
- locator ช่วย: หมวด `select:has(option[value="c0"])` · ฟอนต์ `select:has(option[value="cordia"])` เดิม/ตอนนี้ปุ่ม `button:hasText("เลือกฟอนต์")` · นิยาย `button:hasText("เลือกนิยาย")`
- เกณฑ์ผ่าน: `document.documentElement.scrollWidth === 390` (มือถือ ไม่มีเลื่อนซ้าย-ขวา) · `pageerror` = 0 · **และเปิดรูปดูด้วยตาจริงทุกครั้ง** (เรื่อง layout/ฟอนต์/ขนาด ตาคนตัดสิน)
- ⚠️ **dev server เคยหลุด (ERR_CONNECTION_REFUSED)** ระหว่างเซสชัน — เช็ค `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` ถ้าไม่ใช่ 200 ให้ `(npm run dev > log 2>&1 &)` แล้วรอ ~10 วิ (อย่ารัน `npm run build` ตอน dev เปิด — .next ชนกัน)

## 📌 บทเรียน / กฎที่ย้ำรอบนี้
- **ตาคนตัดสิน (layout/ขนาด/ฟอนต์/ล้นจอ) ต้องเปิด screenshot จริง** — scrollWidth ผ่านอย่างเดียวไม่พอ · พี่กันจะติจากรูปที่เห็น (เช่น "กรอบอวบกว่า" "เล็กจัง")
- **สงสัยสโคป = ถามก่อน** (ใช้ AskUserQuestion) — รอบนี้ถาม 3 ครั้ง (จอคอมหมวดคงชิปหรือ dropdown · แถบตรึงแค่ไหน · ไอคอนยุบ-กางเอาแค่ไหน) กันทำผิดทางเสียแรง
- **แยก 2 ทางด้วย `S.isMobile`** เมื่อ layout ต่างกัน · ถ้าต้องสลับลำดับ element ให้เรนเดอร์ 2 ชิ้นแยก (กันจอคอมเพี้ยน) · ปุ่ม dropdown ทำเป็นโครงเดียวกัน (novelPicker/fontPicker) กรอบจะเท่ากันเอง
- **ลำดับ push เป๊ะ:** ① อัปเดต CLAUDE.md (+README) ก่อน commit → ② commit ละเอียด file-by-file → ③ พี่กันพิมพ์ "พุช" แล้วขึ้น **main เท่านั้น** + sync branch `claude/read-all-files-5lerkm` ให้ตรง (กัน GitHub เห็นแยก) → ④ เขียน SESSION นี้แล้ว push ตาม
- **git "Unverified" บน GitHub** = เครื่องคลาวด์ไม่มีกุญแจเซ็น (committer email = noreply@anthropic.com ถูกแล้ว) → ไม่ใช่ error แก้ฝั่งนี้ไม่ได้ · stop-hook เตือนทุกครั้ง ไม่ต้อง reset-author (ไม่ช่วย)

## สถานะจบรอบนี้
- 5 commit อยู่บน **main** แล้ว · `origin/main` = `origin/claude/read-all-files-5lerkm` = local ตรงกันหมด ไม่ค้าง ไม่แยก
- ไม่มีงานแอปค้าง · เว็บจริง (deploy จาก main) อัปเดตตามหลัง build เสร็จ

## ต่อไป / ค้าง
- ไม่มีงานค้าง · รอพี่กันสั่งงานถัดไป
- ถ้าจะเทส AI รอบหน้า → เปิด `docs/AI-MODEL-TEST.md` อ่านก่อน (ชุดคำเวอร์ชัน 1 · จุดเพ่งเล็ง = พิสูจน์เรื่องลำดับช่องใน JSON = ลำดับการคิดของ AI)
